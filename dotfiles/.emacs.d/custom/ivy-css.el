;;; ivy-css -- Ivy Witter's CSS configuration
;;; Commentary:
;; this is a configuration file
;;
;;; Code:

;;
;; CSS mode configuration
;;
(use-package css-mode
  :ensure nil  ; built-in
  :custom
  (css-indent-offset 2))

(provide 'ivy-css)
;;; ivy-css.el ends here
